services:
  - type: web
    name: tc-profile-video
    runtime: docker
    repo: https://github.com/<you>/topcoder-profile-video-pipeline
    plan: free
    envVars:
      - key: HF_API_TOKEN
        sync: false
      - key: MAX_UPLOAD_MB
        value: "50"
